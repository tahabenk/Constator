<h1>Editing Report</h1>

<% @report.signatures.each do |report| %>
  <%= cl_image_tag report.key, height: 300, width: 400, crop: :fill %>
<% end %>

<%= render 'form', report: @report %>

<%= link_to 'Show', @report %> |
<%= link_to 'Back', reports_path %>
